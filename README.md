IG Session Generator
--------------------
Simple Express app that logs into Instagram (via instagram-private-api) and serializes session state to sessions/<username>-session.json

Usage:
  npm install
  npm start
  Visit http://localhost:3000
